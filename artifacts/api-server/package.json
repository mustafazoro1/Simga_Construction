{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT}",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT}",
    "build": "echo 'no build step for FastAPI server'"
  }
}
